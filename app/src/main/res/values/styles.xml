<resources>

    <!-- Uygulama Teması -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Varsayılan renkler tanımlı değilse buraya eklemiyoruz -->
        <!-- İsteğe bağlı olarak kendi renklerinizi tanımlayabilirsiniz -->
    </style>

    <!-- Saydam Aktivite Teması -->
    <style name="TransparentActivityTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="RoundedImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>


</resources>